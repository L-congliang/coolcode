{
  "category": "context-compression",
  "suite": "context_compression",
  "episode_id": "context_episode_03",
  "fixture": "fixtures/specialized_context_03",
  "prelude_prompts": [
    "Read specs/context.txt completely. Identify the one ACTIVE RULE and retain its exact RULE_ID and formula. Do not edit files yet.",
    "Read diagnostics.log and src/logic.py. Explain which active rule the implementation violates. Do not edit files yet."
  ],
  "prompt": "Now fix src/logic.py using the active rule you found earlier. Run python -m unittest discover -s tests and stop when it passes.",
  "success": {
    "commands": [
      "python -m unittest discover -s tests"
    ],
    "file_contains": {
      "src/logic.py": [
        "RULE_ID = \"CTX_03\"",
        "return value * 4 + 3"
      ]
    },
    "expected_modified": [
      "src/logic.py"
    ],
    "forbidden_modified": [
      "tests/test_logic.py"
    ]
  },
  "oracle": {
    "file_replacements": [
      {
        "file_path": "src/logic.py",
        "old": "RULE_ID = \"UNSET\"\n\ndef transform(value):\n    return value",
        "new": "RULE_ID = \"CTX_03\"\n\ndef transform(value):\n    return value * 4 + 3"
      }
    ]
  },
  "metadata": {
    "rule_id": "CTX_03"
  },
  "id": "context_compression_03_off",
  "variant": "compression_off",
  "agent": {
    "permission_mode": "acceptEdits",
    "max_turns": 18,
    "max_cost_usd": 0.6,
    "thinking": false,
    "compression_mode": "off",
    "effective_context_window": 7000,
    "memory_enabled": false
  },
  "trace_expectations": {
    "required_tools": [
      "read_file",
      "edit_file",
      "run_shell"
    ],
    "min_compression_events": 0,
    "max_tool_calls": 20
  }
}
